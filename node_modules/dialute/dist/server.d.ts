import express = require('express');
import { DialogManager } from './dialog';
export declare class Dialute {
    dm: DialogManager;
    app: express.Express;
    port: string;
    constructor({ dm, port }: {
        dm: DialogManager;
        port: string;
    });
    static fromEntrypoint(entrypoint: GeneratorFunction): Dialute;
    shareApp(path: string): this;
    start(): void;
}
